# ============================================================
# FIXED BETS RO 🇷🇴 — Railway Deployment
# Minimal Nixpacks config — Dockerfile is primary
# ============================================================

# PHP 8.2 with Apache
providers = ["php"]

[php]
version = "8.2"
extensions = ["pdo", "pdo_mysql", "mbstring", "session", "ctype"]
